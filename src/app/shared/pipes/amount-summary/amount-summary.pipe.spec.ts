import { AmountSummaryPipe } from './amount-summary.pipe';

describe('AmountSummaryPipe', () => {
  it('create an instance', () => {
    const pipe = new AmountSummaryPipe();
    expect(pipe).toBeTruthy();
  });
});
