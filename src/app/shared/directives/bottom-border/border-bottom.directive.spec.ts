import { BorderBottomDirective } from './border-bottom.directive';

describe('BorderBottomDirective', () => {
  it('should create an instance', () => {
    const directive = new BorderBottomDirective();
    expect(directive).toBeTruthy();
  });
});
